════════════════════════════════════════════════════════════════
  HLYNR REPLAY SYSTEM - CONTROLS GUIDE
════════════════════════════════════════════════════════════════

🎮 PLAYBACK CONTROLS
────────────────────────────────────────────────────────────────
  NOTE: Replay starts PAUSED - press SPACE to begin playback

  SPACE       Play / Pause
  R           Restart replay (keeps camera where it is)
  Shift+R     Restart replay AND reset camera to initial position
  Ctrl+R      Full scene reload (resets everything)
  ← →         Step backward/forward one frame (when paused)
  H           Hide/Show overlay (toggles HUD)

⏩ SPEED CONTROLS
────────────────────────────────────────────────────────────────
  1           0.1x  (Very Slow - Ultra Slow Motion)
  2           0.25x (Slow - Detailed Analysis)
  3           0.5x  (Half Speed)
  4           1.0x  (Normal Speed)
  5           2.0x  (Double Speed)
  6           4.0x  (Fast Forward)

  [           Decrease speed by 0.25x
  ]           Increase speed by 0.25x

  Range: 0.1x - 10x

📹 CAMERA MODES
────────────────────────────────────────────────────────────────
  I           Toggle Follow Interceptor (chase cam behind blue)
  M           Toggle Follow Threat/Missile (chase cam behind red)
  F           Free Camera mode (unlock, manual control)

  NOTE: Press same key again to unlock and return to free camera

📹 FREE CAMERA CONTROLS (TRUE GOD MODE)
────────────────────────────────────────────────────────────────
  ✨ Camera works EVEN WHEN PAUSED - Full free-fly at all times!

  W/A/S/D     Move forward/left/back/right
  Q/E         Move down/up
  Mouse       Look around (free rotation)
  Scroll      Adjust movement speed (just like Unity Editor!)
              Scroll Up = Faster, Scroll Down = Slower
  Shift       Fast movement (hold while moving)
  ESC         Show/unlock cursor (to interact with UI)

💡 TIPS & TRICKS
────────────────────────────────────────────────────────────────
  • Follow Mode Magic: Press I to follow interceptor, watch it
    chase the target! Press I again to unlock and fly freely

  • Cinematic Replays: Use M to follow the threat, see from its
    perspective as it gets hunted down

  • Multi-Angle Viewing: Position camera, press R to restart
    from that exact angle. No camera reset! Perfect for analysis

  • Pause Analysis: Hit SPACE to freeze, camera still works!
    Fly around frozen action from any angle

  • Slow Motion: Use keys 1-3 for detailed missile behavior
    observation in slow-mo

  • Frame-by-Frame: Pause, then use ← → to step through
    individual timesteps for precise inspection

  • Dynamic Camera Speed: Scroll wheel adjusts speed on-the-fly!
    Start slow for precision, speed up when crossing distances

  • Clean View: Press H to hide overlay for recording/screenshots

  • Full Reset: Shift+R resets both replay AND camera to start

  • Realistic Orientation: Missile meshes automatically orient their
    nose (forward Z+ axis) along the flight path direction!

📊 ON-SCREEN DISPLAY
────────────────────────────────────────────────────────────────
  When overlay is visible (press H to toggle):

  • Episode ID and outcome (green=intercepted, red=miss)
  • Current time / Total duration
  • Playback speed and pause state
  • Frame count and replay modes
  • Performance metrics: fuel used, final distance, total reward

📁 FILE SETUP
────────────────────────────────────────────────────────────────
  Replay files: Assets/StreamingAssets/Replays/*.jsonl
  Set path in ReplayDirector component → episodePath field

════════════════════════════════════════════════════════════════